node_modules/*
test-results/*

# Generated by `npm run codemap` (scripts/generate-codemap.ts) and verified by `codemap:check`.
# Prettier must NOT reformat it: its layout differs from the generator's output, so letting
# both touch the file creates a format/codemap ping-pong where the two checks can never both pass.
CODEMAP.md